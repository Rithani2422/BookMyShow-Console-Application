package BMSPROJECT;  // Declares the package name

public class Helper {  // Defines a public class named 'Helper'
    public static void main(String[] Args) {  // The main method, which serves as the entry point of the program
        BMSAction.start();  // Calls the 'start' method from the 'BMSAction' class
    }
}

